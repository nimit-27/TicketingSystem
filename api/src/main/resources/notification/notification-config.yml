notification:
  supportEmail: jain.nimit99@gmail.com
  senderEmail: jain.nimit99@gmail.com
  enabled: true
  freemarker:
    templateUpdateDelayMs: 60000
    cacheStrongSize: 50
    cacheSoftSize: 500
  email-dispatcher:
    batchSize: 200
    maxRetries: 5
    retryBaseSeconds: 30
    retryMaxSeconds: 3600
    processingTimeoutMinutes: 10
    fixedDelayMs: 5000
    initialDelayMs: 5000
    rateLimitPerSecond: 20
    executorCorePoolSize: 4
    executorMaxPoolSize: 8
    executorQueueCapacity: 500
  templates:
    email: email
    sms: sms
#  supportEmail:

spring:
  mail:
    properties:
      mail.smtp.connectiontimeout: 5000
      mail.smtp.timeout: 5000
      mail.smtp.writetimeout: 5000

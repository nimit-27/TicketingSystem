# Node.js UI
ui/node_modules/
ui/dist/
ui/build/

# Java API (Gradle)
api/build/
api/.gradle/
api/.idea/
api/*.iml
api/*.log

# Docker volumes and containers
/Typesense API/data/
/Typesense API/data/**
*.pid
*.log

# Typesense config overrides (if generated locally)
typesense.ini
config/*.local.*

# Backup files or exported indexes
*.snapshot.json
*.backup.json

# System files
.DS_Store
Thumbs.db

# Node or Python wrapper stuff (if applicable)
node_modules/
.env

# Docker files not to track (if generated)
docker-compose.override.yml

# Filegator files
filegator/filegator.tar
